(ns animation.dungeons-generation
  "Generate dungeons")

(comment
  "Datastructure Goal:
   - A tree with junction distance as metadata
   - As Left leaf largest sized node / right rest
    (Maybe use something like binnary tree ?)
   - Recursively apply depth according parameter")

(defn generate-basemap [width height]
  {})

(defn generate-dungeon [width height depth-thresold]
  "")
